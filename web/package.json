{
  "name": "concertcutter-web",
  "private": true,
  "version": "3.2.0",
  "type": "module",
  "description": "Interface de ConcertCutter, servie par le serveur local Python.",
  "scripts": {
    "test": "node --conditions=browser --import ./tests/register.mjs --test tests/*.test.mjs",
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.56.10",
    "svelte-check": "^4.3.3",
    "typescript": "^5.9.3",
    "vite": "^8.2.2"
  }
}
